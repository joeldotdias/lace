let sentence = "May the lace code run";
let words = split(sentence, " ");
write(words);

let do_func = fn(a, b, f) {
    f(a, b);
};
let sum = do_func(2, 4, fn(x, y) {
    x + y
});
write("Sum is " + sum);
